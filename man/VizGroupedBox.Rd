% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Viz.R
\name{VizGroupedBox}
\alias{VizGroupedBox}
\title{Visualize Grouped Boxplots with Adjusted P-values (Native Implementation)}
\usage{
VizGroupedBox(
  data,
  x_col,
  y_col,
  group_col,
  test_method = "t.test",
  p_adjust_method = "bonferroni",
  symnum.args = list(cutpoints = c(0, 1e-04, 0.001, 0.01, 0.05, 1), symbols = c("****",
    "***", "**", "*", "ns")),
  pal = NULL,
  add_dots = TRUE,
  jitter.size = 1.5,
  jitter.alpha = 0.6,
  box.width = 0.6,
  legend.position = "top",
  title = NULL,
  xlab = "",
  ylab = "Expression Level",
  font.size = 15
)
}
\arguments{
\item{data}{A data frame.}

\item{x_col}{String. Column name for the X-axis (e.g., "Gene").}

\item{y_col}{String. Column name for the Y-axis value (e.g., "Expression").}

\item{group_col}{String. Column name for the grouping variable (e.g., "Condition").}

\item{test_method}{String. "t.test" (default) or "wilcox.test".}

\item{p_adjust_method}{String. Correction method, passed to \code{p.adjust}. Default "bonferroni" or "BH".}

\item{symnum.args}{List. Arguments for significance symbols. Default list(cutpoints = c(0, 0.0001, 0.001, 0.01, 0.05, 1), symbols = c("\emph{\emph{\strong{", "}}", "**", "}", "ns")).}

\item{pal}{Character vector. Custom color palette. Default is c("#B2DF8A", "#33A02C").}

\item{add_dots}{Logical. Whether to show jitter points. Default TRUE.}

\item{jitter.size}{Numeric. Size of jitter points. Default 1.5.}

\item{jitter.alpha}{Numeric. Transparency of jitter points. Default 0.6.}

\item{box.width}{Numeric. Width of boxplots. Default 0.6.}

\item{legend.position}{String or Vector. Position of the legend. Default "top". Examples: "right", "none", c(0.8, 0.9).}

\item{title}{String. Plot title.}

\item{xlab}{String. X-axis label.}

\item{ylab}{String. Y-axis label.}

\item{font.size}{Numeric. Font size for axis text. Default 15.}
}
\value{
A ggplot object.
}
\description{
A wrapper to create grouped boxplots (e.g., Genes on X, Conditions as Groups)
with manually calculated statistical tests. Uses native ggplot2 layers for significance bars
to avoid compatibility warnings and ensure precise alignment.
}
\examples{
\dontrun{
  # 1. 默认图例在顶部
  VizGroupedBox(df, x_col = "gene", y_col = "value", group_col = "group")

  # 2. 图例在右侧
  VizGroupedBox(df, x_col = "gene", y_col = "value", group_col = "group",
                legend.position = "right")

  # 3. 隐藏图例
  VizGroupedBox(df, x_col = "gene", y_col = "value", group_col = "group",
                legend.position = "none")
}
}
