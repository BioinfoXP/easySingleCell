% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/3.scRNA-GSEA.R
\name{scGSEA}
\alias{scGSEA}
\title{Run GO GSEA from single-cell differential expression}
\usage{
scGSEA(
  object = NULL,
  ident.1 = NULL,
  ident.2 = NULL,
  group.by = NULL,
  subset.ident = NULL,
  assay = NULL,
  slot = "data",
  min.pct = 0.1,
  logfc.threshold = 0,
  test.use = "wilcox",
  latent.vars = NULL,
  markers = NULL,
  gene_col = NULL,
  rank.by = "auto",
  species = c("human", "mouse", "rat"),
  org_db = NULL,
  OrgDb = NULL,
  ont = "BP",
  key_type = "SYMBOL",
  keyType = NULL,
  p_cutoff = 0.05,
  pvalueCutoff = NULL,
  min_size = 10,
  minGSSize = NULL,
  max_size = 500,
  maxGSSize = NULL,
  seed = 123,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{object}{A Seurat object. Required when \code{markers = NULL}.}

\item{ident.1}{Identity class, group name, or cell vector passed to
\code{Seurat::FindMarkers()}.}

\item{ident.2}{Optional second identity class, group name, or cell vector.}

\item{group.by}{Optional metadata column used to regroup cells before
differential expression.}

\item{subset.ident}{Optional identity class to subset before regrouping.}

\item{assay}{Assay used by \code{Seurat::FindMarkers()}. Default uses Seurat's
active assay.}

\item{slot}{Expression slot used by \code{Seurat::FindMarkers()}. Default
\code{"data"}.}

\item{min.pct}{Minimum detection fraction passed to \code{FindMarkers()}.}

\item{logfc.threshold}{LogFC threshold passed to \code{FindMarkers()}. Default
\code{0} keeps genes available for pre-ranked GSEA.}

\item{test.use}{Differential expression test passed to \code{FindMarkers()}.}

\item{latent.vars}{Optional latent variables passed to \code{FindMarkers()}.}

\item{markers}{Optional marker table from \code{FindMarkers()} or similar.}

\item{gene_col}{Optional column containing gene symbols or IDs. If \code{NULL},
row names are used when available, otherwise common columns such as
\code{gene} or \code{symbol} are detected.}

\item{rank.by}{Ranking column. Use \code{"auto"} to prefer Seurat logFC columns
such as \code{avg_log2FC}, then \code{avg_logFC}, then \code{stat}.}

\item{species}{One of \code{"human"}, \code{"mouse"}, or \code{"rat"} when \code{OrgDb} is not
supplied.}

\item{org_db}{Optional organism annotation database.}

\item{OrgDb}{ClusterProfiler-style alias for \code{org_db}.}

\item{ont}{GO ontology: \code{"BP"}, \code{"MF"}, \code{"CC"}, or \code{"ALL"}.}

\item{key_type}{Input gene ID key type. Default \code{"SYMBOL"}.}

\item{keyType}{ClusterProfiler-style alias for \code{key_type}.}

\item{p_cutoff}{GSEA p-value cutoff.}

\item{pvalueCutoff}{ClusterProfiler-style alias for \code{p_cutoff}.}

\item{min_size}{Minimum gene set size.}

\item{minGSSize}{ClusterProfiler-style alias for \code{min_size}.}

\item{max_size}{Maximum gene set size.}

\item{maxGSSize}{ClusterProfiler-style alias for \code{max_size}.}

\item{seed}{Random seed used by \code{gseGO()}.}

\item{verbose}{Logical. Passed to Seurat and clusterProfiler.}

\item{...}{Additional arguments passed to \code{Seurat::FindMarkers()} when
\code{markers = NULL}.}
}
\value{
An \code{easy_scGSEA} list with \code{gsea}, \code{markers}, \code{gene_rank}, and
\code{params}.
}
\description{
A Seurat-oriented wrapper that runs \code{Seurat::FindMarkers()}, converts the
marker table into a pre-ranked gene vector, and then runs
\code{clusterProfiler::gseGO()}. If a marker table is already available, pass it
through \verb{markers =} to reuse the ranking and GSEA step without rerunning
differential expression.
}
\examples{
\dontrun{
gsea_res <- scGSEA(
  object = sce,
  ident.1 = "Tumor",
  ident.2 = "Normal",
  group.by = "group",
  species = "human",
  ont = "BP"
)

head(gsea_res$gsea)
head(gsea_res$markers)
}
}
