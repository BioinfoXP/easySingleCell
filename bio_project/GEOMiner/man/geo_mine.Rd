% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{geo_mine}
\alias{geo_mine}
\title{Run GEO Data Miner (CLI Version)}
\usage{
geo_mine(
  keyword,
  context = "",
  limit = 10,
  output_file = "geo_results.xlsx",
  cores = parallelly::availableCores() - 1,
  api_key = Sys.getenv("OPENAI_API_KEY"),
  base_url = "https://api.gpt.ge/v1",
  model = "gpt-4o-mini"
)
}
\arguments{
\item{keyword}{String. The main topic (e.g., "HCC immunotherapy").}

\item{context}{String. Detailed criteria for scoring (e.g., "Must be clinical tissue").}

\item{limit}{Integer. Max number of datasets to analyze. Default 10.}

\item{output_file}{String. Path to save Excel file. If NULL, returns data frame only.}

\item{cores}{Integer. Number of CPU cores. Default is auto-detect.}

\item{api_key}{String. LLM API Key. Defaults to Sys.getenv("OPENAI_API_KEY").}

\item{base_url}{String. API Endpoint. Default "https://api.openai.com/v1".}

\item{model}{String. Model name. Default "gpt-4o-mini".}
}
\value{
A data frame containing the mining results.
}
\description{
Run GEO Data Miner (CLI Version)
}
\examples{
\dontrun{
# 1. Simple search for HCC immunotherapy
results <- geo_mine(
  keyword = "HCC immunotherapy",
  limit = 5,
  api_key = "sk-..."
)

# 2. Advanced search with context and DeepSeek API
# This searches for lung cancer data specifically looking for EGFR mutations
# and excluding cell lines.
results_deepseek <- geo_mine(
  keyword = "Lung Cancer",
  context = "Must be EGFR mutation positive. Tissue samples only. No cell lines.",
  limit = 20,
  output_file = "lung_egfr_results.xlsx",
  base_url = "https://api.deepseek.com/v1",
  model = "deepseek-chat",
  api_key = "sk-..."
)
}
}
