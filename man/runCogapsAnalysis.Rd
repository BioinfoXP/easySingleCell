% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/processScRNAWithGoGAPS.R
\name{runCogapsAnalysis}
\alias{runCogapsAnalysis}
\title{Run CoGAPS Analysis on Single-cell Data}
\usage{
runCogapsAnalysis(
  scRNA,
  nPatterns,
  out_dir,
  nIterations = 1000,
  seed = 42,
  nThreads = 4
)
}
\arguments{
\item{scRNA}{A Seurat object containing single-cell data.}

\item{nPatterns}{A numeric value specifying the number of patterns to be used in CoGAPS analysis.}

\item{out_dir}{A character string specifying the output directory where results will be saved.}

\item{nIterations}{A numeric value specifying the number of iterations for CoGAPS (default is 1000).}

\item{seed}{A numeric value specifying the random seed for reproducibility (default is 42).}

\item{nThreads}{A numeric value specifying the number of threads to be used (default is 4).}
}
\value{
The CoGAPS result object.
}
\description{
This function runs CoGAPS analysis on single-cell data for a specified pattern number and saves the results.
}
\examples{
\dontrun{
library(Seurat)
library(CoGAPS)
library(parallel)

# Example Seurat object
seurat_obj <- CreateSeuratObject(counts = matrix(rnorm(1000), nrow = 100, ncol = 10))
seurat_obj <- NormalizeData(seurat_obj)
seurat_obj <- FindVariableFeatures(seurat_obj)
seurat_obj <- ScaleData(seurat_obj)
seurat_obj <- RunPCA(seurat_obj)
seurat_obj <- RunUMAP(seurat_obj, dims = 1:10)

# Run CoGAPS analysis
runCogapsAnalysis(
  scRNA = seurat_obj,
  nPatterns = 4,
  out_dir = './output_data/Figure7/cogaps',
  nIterations = 1000,
  seed = 42,
  nThreads = 4
)
}
}
