% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Depmap.R
\name{DepmapScatter}
\alias{DepmapScatter}
\title{Visualize Expression vs. Effect Correlation (With Filtering)}
\usage{
DepmapScatter(
  genes,
  rdata_path = "./Depmap/DepMap_Processed.Rdata",
  data_type = c("effect", "dependency"),
  cell_filter = NULL,
  subset_ids = NULL,
  cor_method = "pearson",
  point.color = "navy",
  point.alpha = 0.5,
  line.color = "orange"
)
}
\arguments{
\item{genes}{A character vector of gene symbols.}

\item{rdata_path}{Path to processed RData.}

\item{data_type}{"effect" or "dependency".}

\item{cell_filter}{(Optional) dplyr-style metadata filter.}

\item{subset_ids}{(Optional) Character vector of ModelIDs.}

\item{cor_method}{"pearson" or "spearman".}

\item{point.color}{Color of points.}

\item{point.alpha}{Transparency (0-1).}

\item{line.color}{Color of regression line.}
}
\value{
A ggplot object.
}
\description{
Plots the correlation between Gene Expression and Gene Effect.
Supports metadata filtering and manual stats calculation.
}
\examples{
\dontrun{
  # 1. Basic Usage
  DepmapScatter(genes = "TP53")

  # 2. Metadata Filtering
  DepmapScatter(genes = "TP53", cell_filter = OncotreeLineage == "Lung")

  # 3. Combine with AI Selection (Unified Naming)
  selected_ids <- DepmapMetaSelect("Lung Adenocarcinoma", "OncotreePrimaryDisease", api_key="...")
  DepmapScatter(genes = "KRAS", subset_ids = selected_ids)
}
}
