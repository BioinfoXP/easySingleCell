% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/3.scRNA-H5ad-Seurat-Trans.R
\name{readH5AD}
\alias{readH5AD}
\title{Read H5AD file to Seurat Object (Fixed Version)}
\usage{
readH5AD(
  file,
  data_type = "X",
  assay = "RNA",
  project = NULL,
  verbose = TRUE,
  import_obsm = TRUE,
  import_uns = FALSE,
  store_non_reduction_obsm_as_meta = TRUE,
  skip_obsm_patterns = c("^spatial$", "fate_prob", "trend", "gene", "impute",
    "expression"),
  reduction_patterns = c("pca", "umap", "tsne", "diffmap", "diffusion", "dm_", "lsi",
    "ica"),
  make_unique_features = TRUE
)
}
\arguments{
\item{file}{Path to the .h5ad file.}

\item{data_type}{Character. Specifies which matrix to use:
\itemize{
\item "X": uses \code{adata.X} (default).
\item "raw": uses \code{adata.raw.X}.
\item "counts", "logcounts", etc.: uses \code{adata.layers['name']}.
If the requested type is not found, it automatically falls back to "X".
}}

\item{assay}{Name of the assay to create in Seurat object. Default is "RNA".}

\item{project}{Project name for the returned Seurat object. If NULL, uses the input file name.}

\item{verbose}{Logical. Print progress messages. Default is TRUE.}

\item{import_obsm}{Logical. Whether to import AnnData \code{obsm} entries.}

\item{import_uns}{Logical. Whether to import AnnData \code{uns} entries into \verb{@misc$uns}.}

\item{store_non_reduction_obsm_as_meta}{Logical. Store non-reduction \code{obsm} matrices in \code{meta.data}.}

\item{skip_obsm_patterns}{Character vector of \code{obsm} name patterns to skip as reductions.}

\item{reduction_patterns}{Character vector of \code{obsm} name patterns to import as reductions.}

\item{make_unique_features}{Logical. Make duplicated feature names unique before creating the Seurat object.}
}
\value{
A Seurat object.
}
\description{
Imports an AnnData object (.h5ad) and converts it to a Seurat object,
preserving counts, metadata, and dimensionality reductions.
}
