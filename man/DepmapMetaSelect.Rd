% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Depmap.R
\name{DepmapMetaSelect}
\alias{DepmapMetaSelect}
\title{AI-Powered Fuzzy Metadata Search}
\usage{
DepmapMetaSelect(
  query,
  col_name,
  rdata_path = "./Depmap/DepMap_Processed.Rdata",
  model = NULL,
  api_key = NULL,
  base_url = NULL,
  endpoint = NULL,
  verbose = TRUE
)
}
\arguments{
\item{query}{String. Fuzzy search term (e.g., "Lung Cancer").}

\item{col_name}{String. Metadata column (e.g., "OncotreePrimaryDisease").}

\item{rdata_path}{Path to processed RData.}

\item{model}{Character or NULL. LLM model name. If \code{NULL}, uses the
internal general AI model from \code{R/AI_config.R}.}

\item{api_key}{OpenAI-compatible API key. Defaults to \code{OPENAI_API_KEY}.}

\item{base_url}{Character or NULL. OpenAI-compatible API base URL. If
\code{NULL}, uses the internal base URL from \code{R/AI_config.R}.}

\item{endpoint}{Character or NULL. One of \code{"auto"}, \code{"chat"}, or
\code{"responses"}. \code{NULL} uses the package default \code{"auto"}.}

\item{verbose}{Print debug info.}
}
\value{
A character vector of ModelIDs.
}
\description{
Performs semantic/fuzzy search within a specific metadata column.
Returns ModelIDs matching the query.
}
\examples{
\dontrun{
  # ========================================================
  # Example: Search and Visualize (Unified Naming)
  # ========================================================

  # 1. Select IDs using AI
  selected_ids <- DepmapMetaSelect(
    query = "Lung Cancer",
    col_name = "OncotreePrimaryDisease",
    api_key = "sk-xxxxxx"
  )

  # 2. Pass selected_ids to Boxplot
  DepmapBox(genes = "TP53", subset_ids = selected_ids)

  # 3. Pass selected_ids to Scatterplot
  DepmapScatter(genes = "KRAS", subset_ids = selected_ids)
}
}
